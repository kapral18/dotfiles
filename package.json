{
  "name": "dotfiles",
  "private": true,
  "description": "Repository-level scripts for kapral18/dotfiles.",
  "scripts": {
    "docs": "pnpm --dir website install --frozen-lockfile && pnpm --dir website start",
    "docs:build": "pnpm --dir website install --frozen-lockfile && pnpm --dir website build",
    "docs:serve": "pnpm --dir website install --frozen-lockfile && pnpm --dir website serve",
    "docs:clean": "pnpm --dir website clear"
  }
}
