# Root package.json exists only for npm script shims.
# Do not create repository-root install artifacts when someone runs `yarn` here.
--install.no-lockfile true
--install.modules-folder /tmp/dotfiles-root-yarn-node_modules
