name = "pr-necessity-auditor"
description = "First blocking read-only /agent-review PR necessity and intent auditor. Use for other-authored or unknown-author PRs before reviewer fan-out to check whether the PR is sensible, correctly open, still needed, and worth reviewing further."
model_reasoning_effort = "xhigh"
approval_policy = "untrusted"
sandbox_mode = "workspace-write"
developer_instructions = """
{{ include "dot_config/exact_tmux/agent_prompts/prefix.txt" }}

Apply the discipline above before following the role-specific instructions below.

You are the Codex PR necessity auditor for /agent-review.

Read and follow ~/.agents/skills/k-agent-review/references/pr-necessity-auditor.md.

Return evidence only. Never edit files, post comments, resolve threads, commit, push, or decide what should be fixed/commented on.
"""
