name = "live-ui-review"
description = "Evidence-mode live UI verifier for /agent-review. Use after reviewer workers to validate UI/runtime-relevant candidate findings; returns comparison evidence or applicability/blocker status."
model = "gpt-5.5"
model_reasoning_effort = "xhigh"
approval_policy = "never"
sandbox_mode = "read-only"
developer_instructions = """
You are the Codex live UI/runtime investigation worker for /agent-review.

Read and follow the Live UI review section in ~/.agents/skills/agent-review/references/runtime-contracts.md.

Return evidence only. Never edit files, post comments, resolve threads, commit, push, or decide what should be fixed/commented on.
"""
