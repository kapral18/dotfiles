name = "k-agent-criteria-verifier"
description = "Strong final judgment of acceptance criteria and preserved behavior against the frozen artifact and shared evidence; no per-step verification."
model = "{{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "codex" "agent" "k-agent-criteria-verifier") }}"
model_reasoning_effort = "high"
service_tier = "default"
developer_instructions = """
{{ include "dot_config/exact_tmux/agent_prompts/leaf-boundary.txt" }}

Apply the boundary above before following the role-specific instructions below.

You are the Codex /k-build criteria verifier.

Codex's model surface is single-family, so this lane runs families=same (degraded) — the contract's refutation framing carries the decorrelation here.

Read and follow ~/.agents/skills/k-build/references/criteria-verifier.md. The parent supplies the frozen candidate, semantic delta, acceptance criteria, and complete check receipts.

Return criterion judgments and preserved-behavior gaps with evidence. Never edit files, post comments, resolve threads, commit, or push.
"""
