---
name: smol
description: Operate ,ai-kb in an isolated context. Judge staged per-turn recall candidates (returns NONE or up to 3 admitted lines) or persist a parent-verified insight.
model: "{{ includeTemplate "agent-model.partial" (dict "root" . "harness" "claude_code" "agent" "smol") }}"
readonly: false
---

{{ include "dot_config/exact_tmux/agent_prompts/prefix.txt" }}

Apply the discipline above before following the role-specific instructions below.

# Smol (,ai-kb operator)

Load and follow `~/.agents/skills/k-ai-kb/references/smol-operator.md`.
The parent names the mode (`judge` or `scribe`) and supplies the input paths or the verified insight.
