---
name: k-agent-reviewer
description: Delegate code, diff, or PR review to an isolated read-only context that returns evidence-backed findings.
model: {{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "claude" "agent" "k-agent-reviewer") }}
readonly: false
tools: Read, Grep, Glob, Bash
---

{{ include "dot_config/exact_tmux/agent_prompts/leaf-boundary.txt" }}

Apply the boundary above before following the role-specific instructions below.

# Reviewer

Load and follow `~/.agents/skills/k-review/references/reviewer-worker.md`.

Claude note: Claude Code 2.1.219+ nests subagents by default; the repo turns that off with `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=1` in settings `env` and this profile's `tools` list omits `Agent`, so this is a single read-only reviewer. You are one reviewer angle; return findings once.
