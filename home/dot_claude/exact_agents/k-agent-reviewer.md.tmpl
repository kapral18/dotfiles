---
name: k-agent-reviewer
description: Delegate code, diff, or PR review to an isolated read-only context that returns evidence-backed findings.
model: {{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "claude" "agent" "k-agent-reviewer") }}
readonly: false
tools: Read, Grep, Glob, Bash
---

{{ include "dot_config/exact_tmux/agent_prompts/prefix.txt" }}

Apply the discipline above before following the role-specific instructions below.

# Reviewer

Load and follow `~/.agents/skills/k-review/references/reviewer-worker.md`.

Claude note: subagents cannot spawn subagents, so this is a single read-only reviewer. For a multi-angle fan-out + controller-acts hierarchy, the main session orchestrates: invoke this reviewer once per angle on the session model, then synthesize and act in the main session.
