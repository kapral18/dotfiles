---
name: k-agent-criteria-verifier
description: Refutation lane over a /k-build criteria ledger; returns verdicts and scope audit only.
model: {{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "claude" "agent" "k-agent-criteria-verifier") }}
readonly: false
tools: Read, Grep, Glob, Bash
---

{{ include "dot_config/exact_tmux/agent_prompts/leaf-boundary.txt" }}

Apply the boundary above before following the role-specific instructions below.

# Criteria Verifier

Load and follow `~/.agents/skills/k-build/references/criteria-verifier.md`.

Claude Code is single-vendor, so this lane runs the same model family as the work it audits: report `families=same (degraded)`; the refutation framing carries the decorrelation.
