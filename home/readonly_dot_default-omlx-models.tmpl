# oMLX model manifest (rendered by chezmoi).
#
# Format (pipe-delimited, one model per line):
#   <hf-repo-id>|<local-dir-name>
#
# - hf-repo-id: Hugging Face repo id (e.g. "NexVeridian/Qwen3.6-35B-A3B-5bit")
# - local-dir-name: subdirectory under ~/.omlx/models/ where weights land
#
# Lines starting with `#` and blank lines are ignored.
#
# Downloads are opt-in via the `downloadOmlxModels` data variable in
# ~/.config/chezmoi/chezmoi.toml. Re-prompt by clearing that value.
#
# Strategy: one local oMLX backend model, small enough for the default work
# profile model-memory budget while retaining more precision than the old
# universal 4-bit tier.

# Qwen3.6 35B-A3B @ 5-bit (~23.8 GB MLX safetensors).
# https://huggingface.co/NexVeridian/Qwen3.6-35B-A3B-5bit
NexVeridian/Qwen3.6-35B-A3B-5bit|qwen3.6-35b-a3b-5bit
