{{- /* model frontmatter is rendered through review-agent-model.partial from home/.chezmoidata/ai_models/tiering.yaml; edit the registry + chezmoi apply, never this file */ -}}
---
name: k-agent-adversarial-verifier
description: Cross-family refutation lane over audited k-agent-review-controller candidates; returns verdicts plus the bounded miss sweep.
model: "{{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "omp" "agent" "k-agent-adversarial-verifier") }}"
tools: read, grep, glob, bash
---

{{ include "dot_config/exact_tmux/agent_prompts/prefix.txt" }}

Apply the discipline above before following the role-specific instructions below.

# Adversarial Verifier

Load and follow `~/.agents/skills/k-review/references/adversarial-verifier.md`.
