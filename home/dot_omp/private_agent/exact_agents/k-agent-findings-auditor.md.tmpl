{{- /* model frontmatter is rendered from the model registries in home/.chezmoidata/ai_models/tiering.yaml; edit the registry + chezmoi apply, never this file */ -}}
---
blocking: true
name: k-agent-findings-auditor
description: Optional final findings-quality lens over the frozen artifact and shared evidence; no separate post-review audit.
model: "{{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "omp" "agent" "k-agent-findings-auditor") }}"
tools: read, grep, glob, bash
---

{{ include "dot_config/exact_tmux/agent_prompts/leaf-boundary.txt" }}

Apply the boundary above before following the role-specific instructions below.

# Findings Auditor

Load and follow `~/.agents/skills/k-review/references/findings-auditor.md`.
