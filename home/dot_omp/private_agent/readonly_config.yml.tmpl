# Managed by chezmoi (source: home/dot_omp/private_agent/readonly_config.yml.tmpl).
# Keep OMP native guidance/features as the base; this file only pins user policy.
#
# modelRoles is profile-independent: both machines ride the ChatGPT-subscription openai-codex
# provider for every role (user call 2026-09-23; was openrouter since 2026-09-07), so the block
# lives outside the isWork branch. Only the web-search chains below still fork on isWork.
#
# Every built-in omp role is pinned explicitly (default/smol/slow/vision/plan/commit/
# tiny/task/advisor per `omp config get modelRoles`); nothing falls through to the
# harness default or the @smol fallback. Roles mirror `category_models.codex`:
# - T1 primaries (default/slow/plan): GPT-6 Sol, :high for default, :max for the deliberate slow/plan
#   lanes. research/review profiles ride @default.
# - vision: GPT-6 Luna at :high (image input; user call 2026-09-23).
# - task (T2 implement: the native `task` agent and every implement worker): GPT-6 Sol at :medium.
# - smol (mechanical/memory): GPT-6 Luna at :high.
# - tiny (titles, memory, auto-thinking classification, unexpected-stop detection) and commit
#   (commit-message generation): GPT-6 Luna at :medium — background chores.
# - advisor: GPT-6 Sol at :high. One OpenAI family only, so refute is degraded (same call as
#   `category_models.codex.refute`).
modelRoles:
  default: openai-codex/gpt-6-sol:high
  smol: openai-codex/gpt-6-luna:high
  slow: openai-codex/gpt-6-sol:max
  vision: openai-codex/gpt-6-luna:high
  plan: openai-codex/gpt-6-sol:max
  commit: openai-codex/gpt-6-luna:medium
  tiny: openai-codex/gpt-6-luna:medium
  task: openai-codex/gpt-6-sol:medium
  advisor: openai-codex/gpt-6-sol:high

modelProviderOrder:
  - anthropic
  - openai-codex
  - openrouter
  - cursor
  - openai

# Background advisor is disabled; the refute role remains available for final verification. omp migrates
# `advisor.subagents` into `task.agentAdvisor.task` (settings.ts) and an explicit
# agentAdvisor entry wins, so both knobs are pinned off below.
advisor:
  enabled: false
  subagents: false
  syncBacklog: 1
  immuneTurns: 0

defaultThinkingLevel: high
extendedContext: true

# omp installs via the unpinned pnpm package in ~/.default-pnpm-pkgs.
# Apply runs pnpm update -g --latest. Startup update check stays off so
# pnpm remains the installer.
startup:
  checkUpdate: false
  quiet: true
  showSplash: false

memory:
  backend: off

autolearn:
  enabled: false
  autoContinue: false

dev:
  autoqaConsent: granted

skills:
  enabled: true
  enableSkillCommands: true

# maxRecursionDepth 1: root may spawn; children get no `task` tool (user call
# 2026-09-07), making the SOP §3.7 leaf-worker boundary mechanical, not instruction-only.
# Explicit root async remains available. Implicit backgrounding also affects
# blocking children, so Bash/Eval must finish in the foreground by default.
async:
  enabled: true

bash:
  autoBackground:
    enabled: false
eval:
  autoBackground:
    enabled: false

task:
  isolation:
    mode: auto
  enableEffort: true
  enableLsp: true
  maxRecursionDepth: 1
  disabledAgents:
    - reviewer
    - security-reviewer
  agentAdvisor:
    task: "off"

retry:
  enabled: true
  maxRetries: 3

symbolPreset: nerd

theme:
  dark: dark-catppuccin
setupVersion: 2
# web_search: unlisted ids stay in the fallback chain, so every unused provider
# is excluded. Listing perplexity is an OpenRouter sonar-pro opt-in. Keyless
# scrapers are duckduckgo/startpage/ecosia/google/mojeek/public. Personal keeps
# those after Codex in OMP's built-in relative order.
# {{ if eq .isWork true }}
providers:
  webSearchOrder:
    - openrouter
    - codex
    - gemini
    - google
    - duckduckgo
  webSearchExclude:
    - perplexity
    - anthropic
    - xai
    - zai
    - exa
    - tinyfish
    - jina
    - kagi
    - tavily
    - firecrawl
    - brave
    - kimi
    - parallel
    - synthetic
    - searxng
    - startpage
    - ecosia
    - mojeek
    - public
# {{ else }}
providers:
  webSearchOrder:
    - codex
    - startpage
    - duckduckgo
    - ecosia
    - google
    - mojeek
    - public
  webSearchExclude:
    - perplexity
    - gemini
    - anthropic
    - xai
    - zai
    - exa
    - tinyfish
    - jina
    - kagi
    - tavily
    - firecrawl
    - brave
    - kimi
    - parallel
    - synthetic
    - searxng
# {{ end }}
composer:
  shape: rule
terminal:
  showProgress: true
display:
  cacheMissMarker: true
steeringMode: one-at-a-time
tools:
  approvalMode: yolo
  approval:
    {}
features:
  unexpectedStopDetection: smart
edit:
  mode: hashline
astGrep:
  enabled: true
generate_image:
  enabled: true
computer:
  enabled: true
checkpoint:
  enabled: true
github:
  enabled: true
grep:
  contextBefore: 1
