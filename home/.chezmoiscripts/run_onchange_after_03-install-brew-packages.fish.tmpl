#!/usr/bin/env fish

echo "---------------------------------------"
echo "brew install from Brewfile: applying..."
echo "---------------------------------------"

# Brewfile hash: {{ includeTemplate "brews/_assemble.brewfile" . | sha256sum }}
set -l agy_target (brew --prefix)/bin/agy
if test -f "$agy_target"; and not test -L "$agy_target"
    brew uninstall --cask --force antigravity-cli >/dev/null 2>&1; or true
    rm -f "$agy_target"
end

brew bundle cleanup --global --force
brew bundle --global --no-upgrade

echo "---------------------------------"
echo "brew install from Brewfile: done "
echo "---------------------------------"
