#!{{ lookPath "fish" }}

echo "---------------------------------------"
echo "brew install from Brewfile: applying..."
echo "---------------------------------------"

echo '
{{ template "Brewfile" . }}
' | brew bundle --no-lock --file=-

echo "---------------------------------------"
echo "brew install from Brewfile: done"
echo "---------------------------------------"


# store an array in fish

set -l cargo_install_packages ast-grep bandwhich cargo-update cargo-watch diskonaut git-brws hackernews_tui lemmeknow oatmeal tokei trippy viu

for package in $cargo_install_packages
    if ! command -v $package &> /dev/null
        echo "-------------------------"
        echo "cargo install $package: applying..."
        echo "-------------------------"
        cargo install $package --locked
        echo "-------------------------"
        echo "cargo install $package: done"
        echo "-------------------------"
    end
end

if ! command -v tlm &> /dev/null
    echo "-------------------------"
    echo "install tlm: applying..."
    echo "-------------------------"
    curl -fsSL https://raw.githubusercontent.com/yusufcanb/tlm/main/install.sh | sudo bash -E
    echo "-------------------------"
    echo "install tlm: done"
    echo "-------------------------"
end

echo ""


