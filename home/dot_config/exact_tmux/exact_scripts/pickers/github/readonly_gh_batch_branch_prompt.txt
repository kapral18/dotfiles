Read the scratch buffer at __SCRATCH__ and fill in the branch names. For each issue row (`<repo>#<number>|`), fetch its title with `gh issue view <number> --repo <repo> --json title --jq '.title'`. Invent a branch name using conventional commit format: type/scope/body with dashes (e.g. fix/console/boolean-suggestion-quoted-string), appending the branch name after the pipe on the same line. Do NOT create any worktrees, branches, or git operations — just edit the file to fill in the names and stop.
