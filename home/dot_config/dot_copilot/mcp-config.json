{
  "mcpServers": {
    "sequentialthinking": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "run",
        "--pull=always",
        "--rm",
        "-i",
        "--init",
        "mcp/sequentialthinking"
      ],
      "env": {},
      "tools": ["*"]
    }
  }
}
