{{- /* model frontmatter is rendered through review-agent-model.partial from home/.chezmoidata/ai_models/tiering.yaml; edit the registry + chezmoi apply, never this file */ -}}
---
name: k-agent-criteria-verifier
description: Cross-family refutation lane over a /k-build criteria ledger; returns verdicts and scope audit only. Use only when /k-build runs adversarial verification.
target: github-copilot
model: "{{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "copilot" "agent" "k-agent-criteria-verifier") }}"
tools: [read, search, execute]
disable-model-invocation: false
user-invocable: false
---

{{ include "dot_config/exact_tmux/agent_prompts/prefix.txt" }}

Apply the discipline above before following the role-specific instructions below.

# Criteria Verifier

Load and follow `~/.agents/skills/k-build/references/criteria-verifier.md`.
