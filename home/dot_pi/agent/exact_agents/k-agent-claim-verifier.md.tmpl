{{- /* model frontmatter is rendered through review-agent-model.partial from home/.chezmoidata/ai_models/tiering.yaml; edit the registry + chezmoi apply, never this file */ -}}
---
name: k-agent-claim-verifier
description: Independent public-claim verification lane for k-public-sources; takes claim + primary source, returns verified/refuted/undecidable only.
model: "{{ includeTemplate "review-agent-model.partial" (dict "root" . "harness" "pi" "agent" "k-agent-claim-verifier") }}"
tools: read, grep, find, ls, bash
systemPromptMode: replace
inheritProjectContext: true
inheritSkills: true
---

{{ include "dot_config/exact_tmux/agent_prompts/leaf-boundary.txt" }}

Apply the boundary above before following the role-specific instructions below.

# Claim Verifier

Load and follow `~/.agents/skills/k-public-sources/references/claim-verifier.md`.
