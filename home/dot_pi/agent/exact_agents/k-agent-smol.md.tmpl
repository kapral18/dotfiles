{{- /* model frontmatter is rendered through agent-model.partial from home/.chezmoidata/ai_models/tiering.yaml; edit the registry + chezmoi apply, never this file */ -}}
---
name: k-agent-smol
description: Operate ,ai-kb in an isolated context. Judge staged per-turn recall candidates or an ad-hoc recall query (returns NONE or up to 3 admitted lines).
model: "{{ includeTemplate "agent-model.partial" (dict "root" . "harness" "pi" "agent" "k-agent-smol") }}"
{{- $thinking := includeTemplate "agent-thinking.partial" (dict "root" . "harness" "pi" "agent" "k-agent-smol") }}
{{- if $thinking }}
thinking: "{{ $thinking }}"
{{- end }}
tools: read, grep, find, ls, bash
systemPromptMode: replace
inheritProjectContext: false
inheritGlobalContext: false
inheritSkills: false
maxSubagentDepth: 0
defaultContext: fresh
---

{{ include "dot_config/exact_tmux/agent_prompts/leaf-boundary.txt" }}

Apply the boundary above before following the role-specific instructions below.

# k-agent-smol (,ai-kb operator)

Load and follow `~/.agents/skills/k-ai-kb/references/smol-operator.md`.
The parent runs judge mode only and supplies the input paths or the recall query.
