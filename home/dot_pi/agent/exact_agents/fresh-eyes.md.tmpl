{{- /* model frontmatter is rendered from the model registries in home/.chezmoidata/ai_models.yaml; edit the registry + chezmoi apply, never this file */ -}}
---
name: fresh-eyes
description: Blind zero-context clarity lane for one review-controller fan-out; returns clarity findings only.
model: "{{ .agent_review_models.pi.lanes }}"
tools: read, grep, find, ls, bash
systemPromptMode: replace
inheritProjectContext: true
inheritSkills: false
---

{{ include "dot_config/exact_tmux/agent_prompts/prefix.txt" }}

Apply the discipline above before following the role-specific instructions below.

# Fresh-Eyes Worker

Load and follow `~/.agents/skills/k-review/references/fresh-eyes.md`.

This profile deliberately carries no skills — the blind lane must not load the `k-review` skill or any PR context. It exists because this harness launches the lane through a named profile; generic-task harnesses carry the same contract in the task prompt.
